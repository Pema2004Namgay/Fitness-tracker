<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Exercise</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f9f9f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }

    .container {
      background-color: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
      max-width: 500px;
      width: 100%;
    }

    h2 {
      color: #722EFA;
      margin-bottom: 20px;
      text-align: center;
    }

    label {
      font-weight: 600;
      display: block;
      margin-top: 15px;
      color: #333;
    }

    input, textarea, button {
      width: 100%;
      padding: 12px;
      margin-top: 8px;
      border: 1px solid #ddd;
      border-radius: 8px;
      font-size: 16px;
      box-sizing: border-box;
    }

    button {
      margin-top: 25px;
      background-color: #722EFA;
      color: #fff;
      font-weight: bold;
      border: none;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #5b24c9;
    }

    .back-link {
      display: block;
      margin-top: 20px;
      text-align: center;
      color: #722EFA;
      text-decoration: none;
      font-size: 14px;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="container">
    <h2>Edit Exercise</h2>

    <form action="/exercise/edit/<%= exercise.id %>" method="POST">
      <label for="title">Title</label>
      <input id="title" name="title" value="<%= exercise.title %>" required>

      <label for="description">Description</label>
      <textarea id="description" name="description" rows="4" required><%= exercise.description || '' %></textarea>

      <label for="duration">Duration (minutes)</label>
      <input id="duration" name="duration" type="number" value="<%= exercise.duration %>" required>

      <label for="date">Date</label>
      <input id="date" name="date" type="date" value="<%= exercise.date.toISOString().substring(0, 10) %>" required>

      <button type="submit">Update</button>
    </form>

    <a class="back-link" href="/exercise">← Back to Exercise List</a>
  </div>

</body>
</html>
